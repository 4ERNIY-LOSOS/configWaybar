{
    "clock": {
        "interval": 60,
        "tooltip": true,
        "format": "{:%H.%M}",
        "tooltip-format": "{:%d-%m-%Y}"
    },
    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-muted": " Muted", // Иконка для "без звука" (fa-volume-slash)
        "format-icons": {
            "default": ["", "", ""] // Иконки для уровней громкости, теперь 3 уровня:
                                               //  (fa-volume-off) ~1-33%
                                               //  (fa-volume-low) ~34-66%
                                               //  (fa-volume-medium) ~67-100%
        },
        "on-click": "pactl set-sink-mute @DEFAULT_SINK@ toggle", // Левый клик - Mute/Unmute
        "on-click-right": "~/.config/waybar/scripts/wofi-sound-sink-selector.sh", // Правый клик - запуск скрипта выбора через wofi
        "on-scroll-up": "pactl set-sink-volume @DEFAULT_SINK@ +5%",
        "on-scroll-down": "pactl set-sink-volume @DEFAULT_SINK@ -5%",
        "scroll-step": 5 // Шаг прокрутки громкости в процентах
    },
    "network": {
        "format-wifi": " {essid} ({signalStrength}%)", // Icon: fa-wifi
        "format-ethernet": " {ifname}", // Icon: fa-link (или  fa-ethernet)
        "format-disconnected": " Disconnected", // Icon: fa-unlink
        "tooltip-format": "{ifname} via {gwaddr} \nIP: {ipaddr}\nMAC: {hwaddr}", // Icon: fa-network-wired
        "on-click-right": "~/.config/waybar/scripts/show-speedtest-notification.sh" // Изменено на вызов скрипта
    }
    // Другие модули будут добавляться сюда
}
